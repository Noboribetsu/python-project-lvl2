name: build & test

on: [push]
jobs:
  build_and_test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - run: python3 -m pip install --user poetry
    - run: make install
    - run: make lint
    - run: make test
